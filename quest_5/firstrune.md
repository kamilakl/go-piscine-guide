# Quest 5.1 First Rune

## Название файла Go
`firstrune.go`

---

## Условия задания 

| **Instruction** | **Перевод** |
|---------------------------|--------------|
| Write a function that returns the first rune of a string. | Напиши функцию, которая возвращает первую руну (символ) строки. |

---

Нужно написать функцию, которая **возвращает первый символ (руну) из строки**.

### Пример

- **Ввод:**
  ```go
  z01.PrintRune(piscine.FirstRune("Hello!"))
  z01.PrintRune(piscine.FirstRune("Salut!"))
  z01.PrintRune(piscine.FirstRune("Ola!"))
  ```
- **Вывод:**  
  ```
  HSO
  ```

---

## Решение

```go
package piscine

func FirstRune(s string) rune {
	for _, r := range s {
		return r
	}
	return 0
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода** | **Объяснение и назначение** |
|-------------------------------|------------------------------|
| **Пакет (`package piscine`)** | Каждая программа в Go делится на пакеты. `package` определяет, к какому модулю относится данный файл. Пакет `piscine` — это, по сути, «папка с функциями». Все файлы внутри одного пакета видят функции друг друга. |
| **Функция (`func FirstRune(s string) rune`)** | Объявление функции. Ключевое слово `func` сообщает компилятору, что дальше идёт функция. Имя `FirstRune` — название функции (с заглавной буквы, значит доступна извне пакета). В скобках указаны параметры `s string`, а после них — тип возвращаемого значения `rune`. |
| **Параметры (`s string`)** | Это входные данные функции. Здесь `s` — строка, из которой нужно взять первый символ. |
| **Цикл (`for _, r := range s`)** | Цикл `for range` используется для перебора строки посимвольно. `_` — место для индекса (он не нужен), `r` — переменная, в которую попадает каждая руна (символ). |
| **Возврат (`return r`)** | Как только найден первый символ, функция сразу возвращает его. После `return` выполнение функции прекращается. |
| **Возврат (`return 0`)** | Если строка пустая, цикл не выполнится, и функция вернёт `0`. |
| **Фигурные скобки `{ }`** | Определяют границы блоков кода (функции, цикла, условий). Всё, что внутри скобок, относится к этому блоку. |

---

## Логика и пошаговый разбор кода

### 1. Объявление функции

```go
func FirstRune(s string) rune {
```
Функция объявляется с именем **FirstRune**, принимает строку `s` и возвращает `rune` — символ Unicode.

---

### 2. Цикл по строке

```go
for _, r := range s {
```
Запускается цикл, который перебирает каждый символ строки `s`.  
Первым делом он получает первую руну и сохраняет её в переменной `r`.

---

### 3. Возврат первого символа

```go
return r
```
После получения первой руны функция сразу её возвращает.  
Так как есть `return`, цикл не выполняется дальше.

---

### 4. Возврат по умолчанию

```go
return 0
```
Если строка окажется пустой, цикл не запустится, и функция вернёт `0` — пустой символ.

---

## Глоссарий

| **Термин** | **Понятное объяснение** |
|-------------|--------------------------|
| **rune** | Тип данных для представления одного символа Unicode. |
| **string** | Последовательность символов (строка). |
| **range** | Ключевое слово для перебора элементов коллекции (строки, массива, карты). |
| **_ (подчёркивание)** | Используется для игнорирования значения (например, индекса в цикле). |
| **return** | Команда, которая возвращает значение из функции и завершает её выполнение. |
| **0 (нулевая руна)** | Символ с кодом 0, обозначает «пустое значение». |
| **цикл for** | Конструкция, которая повторяет действия. В Go это универсальный цикл. |
| **package** | Группа связанных файлов и функций в Go. |

---

## Заключение

Функция **FirstRune** принимает строку и возвращает **первый символ (руну)**.

Она использует цикл `for range`, чтобы пройти по строке, берёт первый элемент и сразу возвращает его.  
Если строка пустая, функция возвращает `0`.

📘 Пример:  
- `"Hello!"` → `H`  
- `"Salut!"` → `S`  
- `"Ola!"` → `O`
