# Quest 5.9 Is Lower

**Автор:** KeShMaNsKy  

---

## Название файла Go
`islower.go`

---

## Условия задания 

| **Instruction** | **Перевод** |
|---------------------------|--------------|
| Write a function that returns true if the string passed as the parameter contains only lowercase characters, otherwise, returns false. | Напиши функцию, которая возвращает `true`, если переданная строка содержит только строчные буквы. В противном случае функция возвращает `false`. |

---

### Пример

- **Ввод:**
  ```go
  fmt.Println(piscine.IsLower("hello"))
  fmt.Println(piscine.IsLower("hello!"))
  ```
- **Вывод:**
  ```
  true
  false
  ```
Дополнительно:
- `IsLower("AZ")` → `false`
- `IsLower("az")` → `true`
- `IsLower("-asdq-qcLEWQV-")` → `false`

---

## Решение

```go
package piscine

func IsLower(s string) bool {
	for _, letter := range s {
		if letter < 'a' || letter > 'z' {
			return false
		}
	}
	return true
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода** | **Объяснение и назначение** |
|-------------------------------|------------------------------|
| **Цикл `for _, letter := range s`** | Перебирает каждый символ строки `s`. `letter` — это руна (`rune`), то есть Unicode-код символа. `_` — символ значит что индекс нам не нужен. |
| **Условие (`if letter < 'a' || letter > 'z'`)** | Проверяет, находится ли символ в диапазоне строчных латинских букв. Если нет возвращает `false`. |
| **Символы `'a'` и `'z'`** | В таблице ASCII `'a'` = 97, `'z'` = 122. Проверка `letter < 'a' || letter > 'z'` исключает все символы вне диапазона `a–z`. |
| **Тип `rune`** | Тип данных в Go, представляющий символ Unicode (`int32`). Используется для работы с буквами, а не с байтами. |

---

## Логика и пошаговый разбор кода

### 1. Объявление функции
```go
func IsLower(s string) bool {
```
Функция принимает строку `s` и возвращает значение типа `bool`.  
Если строка состоит только из строчных латинских букв, вернётся `true`. Иначе — `false`.

---

### 2. Перебор символов строки
```go
for _, letter := range s {
```
Цикл `for range` проходит по всем символам строки.  
Каждый символ (`letter`) представлен как руна (`rune`), то есть числовое значение символа.

---

### 3. Проверка диапазона символов
```go
if letter < 'a' || letter > 'z' {
	return false
}
```
- Проверяется, находится ли символ в диапазоне `'a'`–`'z'`.  
- `'a'` имеет значение 97, `'z'` — 122 в ASCII.  
- Если символ выходит за эти пределы (например, пробел, цифра, заглавная буква или пунктуация) — возвращается `false`.

---

### 4. Возврат результата
```go
return true
```
Если цикл завершился и не было найдено ни одного неподходящего символа, функция возвращает `true`.

---

## Важно знать

> В Go строки состоят из байт, а не из символов.  
> Чтобы работать с буквами, особенно Unicode, нужно использовать **руны (`rune`)**.  
> Цикл `for range` делает это автоматически.

📘 Пример:
```go
for _, letter := range "hello" {
	fmt.Println(letter)
}
```
Выводит Unicode-коды:
```
104 101 108 108 111
```
Которые соответствуют символам `'h'`, `'e'`, `'l'`, `'l'`, `'o'`.

---

## Глоссарий

| **Термин** | **Пояснение** |
|-------------|----------------|
| **ASCII** | Таблица, где каждому символу присвоен числовой код. `'a'` = 97, `'z'` = 122. |
| **Unicode** | Глобальный стандарт кодировки символов, включающий все языки мира. |
| **rune** | Тип данных (`int32`), представляющий один символ Unicode. |
| **bool** | Логический тип данных (`true` или `false`). |
| **if / return** | Конструкция для проверки условий и возврата результата из функции. |
| **for range** | Цикл, который перебирает элементы строки, массива, среза и т.д. |

---

## Заключение
Алгоритм:
1. Пройти по каждому символу строки превратив его в руну.  
2. Проверить, находится ли он между `'a'` и `'z'`.  
3. Если хоть один символ не подходит — вернуть `false`.  
4. Если все символы подходят — вернуть `true`.
