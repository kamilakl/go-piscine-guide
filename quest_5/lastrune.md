# Quest 5.2 Last Rune

## Название файла Go
`lastrune.go`

---

## Условия задания 

| **Instruction** | **Перевод** |
|---------------------------|--------------|
| Write a function that returns the last rune of a string. | Напиши функцию, которая возвращает последнюю руну (символ) строки. |

---

### Пример

- **Ввод:**
  ```go
  z01.PrintRune(piscine.LastRune("Hello!"))
  z01.PrintRune(piscine.LastRune("Salut!"))
  z01.PrintRune(piscine.LastRune("Ola!"))
  ```
- **Вывод:**  
  ```
  !!!
  ```

---

## Решение

```go
package piscine

func LastRune(s string) rune {
	var last rune
	for _, r := range s {
		last = r
	}
	return last
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода** | **Объяснение и назначение** |
|-------------------------------|------------------------------|
| **Пакет (`package piscine`)** | Каждая программа в Go делится на пакеты. `package` определяет, к какому модулю относится данный файл. Пакет `piscine` — это, по сути, «папка с функциями». Все файлы внутри одного пакета видят функции друг друга. |
| **Функция (`func LastRune(s string) rune`)** | Объявление функции. Ключевое слово `func` сообщает компилятору, что далее идёт функция. Имя `LastRune` — название функции (с заглавной буквы, значит она экспортируется и доступна извне пакета). В скобках указаны параметры `s string`, а после них — тип возвращаемого значения `rune`. |
| **Параметры (`s string`)** | Это входные данные функции. Здесь `s` — строка, из которой нужно взять последний символ. |
| **Переменная (`var last rune`)** | Создаёт переменную `last` типа `rune`. Она предназначена для хранения текущего символа во время перебора строки. В конце цикла в ней останется последняя руна. |
| **Цикл (`for _, r := range s`)** | Цикл `for range` используется для перебора каждого символа строки `s`. `_` — индекс (не используется), `r` — текущая руна. |
| **Присваивание (`last = r`)** | На каждом шаге цикла текущее значение `r` записывается в переменную `last`. Таким образом, `last` всегда содержит последнюю встреченную руну. |
| **Возврат (`return last`)** | После завершения цикла функция возвращает значение переменной `last`, которое содержит последнюю руну строки. |
| **Фигурные скобки `{ }`** | Определяют границы блоков кода: тела функции, цикла, условий. Всё, что внутри скобок, принадлежит этому блоку. |

---

## Логика и пошаговый разбор кода

### 1. Объявление функции

```go
func LastRune(s string) rune {
```
Создаётся функция **LastRune**, которая принимает строку `s` и возвращает одну руну (символ).

---

### 2. Создание переменной для хранения символа

```go
var last rune
```
Объявляем переменную `last`, куда будем записывать символ на каждом шаге цикла.  
Сначала она пустая, потом постепенно обновляется.

---

### 3. Перебор строки посимвольно

```go
for _, r := range s {
	last = r
}
```
Цикл `for range` проходит по всем символам строки.  
На каждой итерации:
- `r` получает текущую руну строки.
- `last` принимает значение этой руны.

Например, если строка `"Hello!"`, то процесс будет таким:

| Итерация | Значение `r` | Значение `last` |
|-----------|---------------|-----------------|
| 1 | `'H'` | `'H'` |
| 2 | `'e'` | `'e'` |
| 3 | `'l'` | `'l'` |
| 4 | `'l'` | `'l'` |
| 5 | `'o'` | `'o'` |
| 6 | `'!'` | `'!'` |

После окончания цикла `last` содержит `'!'` — последнюю руну строки.

---

### 4. Возврат результата

```go
return last
```
Когда цикл завершён, функция возвращает `last` — последний символ строки.

---

## Глоссарий

| **Термин** | **Понятное объяснение** |
|-------------|--------------------------|
| **rune** | Тип данных в Go для представления одного символа Unicode. |
| **string** | Последовательность символов (строка). |
| **range** | Ключевое слово для перебора элементов коллекций (строк, массивов, карт). |
| **_ (подчёркивание)** | Специальный идентификатор, который используется для игнорирования значения (например, индекса). |
| **return** | Оператор, который завершает выполнение функции и возвращает значение. |
| **var** | Ключевое слово для объявления переменной. |
| **0 (нулевая руна)** | Начальное значение переменной типа `rune`, обозначающее «ничего». |
| **цикл for** | Универсальный цикл для повторения действий в Go. |
| **package** | Группа файлов и функций, объединённых в один модуль. |

---

## Заключение

Функция **LastRune** принимает строку и возвращает **последнюю руну (символ)**.  
Она проходит по всей строке при помощи цикла `for range`, обновляя значение переменной `last` на каждом шаге.  
Когда цикл заканчивается, в `last` остаётся последняя руна строки — и именно она возвращается.

📘 Пример работы функции:  
- `"Hello!"` → `'!'`  
- `"Salut!"` → `'!'`  
- `"Ola!"` → `'!'`  

Таким образом, **LastRune** — это простая и надёжная функция для получения последнего символа из строки в Go.
