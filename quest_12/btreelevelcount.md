# Quest 12.6. btreelevelcount

## Название файла Go

`btreelevelcount.go`

---

## Условия задания

| **Instruction**                                                                                                 | **Перевод**                                                                              |
| --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| *Write a function, BTreeLevelCount, that returns the number of levels of the binary tree (height of the tree).* | *Напишите функцию, которая возвращает количество уровней бинарного дерева (его высоту).* |

---

Нужно написать функцию, которая определяет **высоту бинарного дерева**, то есть количество уровней — сколько "слоёв" узлов от корня до самого глубокого листа.

📌 **Важно:**
Высота дерева = количество уровней
Уровни считаются начиная **с 1**:

```
Level 1 → корень
Level 2 → дети корня
Level 3 → внуки корня
...
```

---

### Пример

* **Ввод:**

Дерево после вставки:

```
        4        ← Level 1
      /   \
     1     7     ← Level 2
          /
         5        ← Level 3
```

* **Вывод:**

```
3
```

---

## Решение

```go
package piscine

func BTreeLevelCount(root *TreeNode) int {
	if root == nil {
		return 0
	}
	leftHeight := BTreeLevelCount(root.Left)
	rightHeight := BTreeLevelCount(root.Right)

	if leftHeight > rightHeight {
		return leftHeight + 1
	}
	return rightHeight + 1
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода**              | **Объяснение и назначение**                                                |
| ------------------------------------------ | -------------------------------------------------------------------------- |
| **Пакет (`package ...`)**                  | Объединяет файл в модуль для импорта.                                      |
| **Высота дерева**                          | Максимальное количество уровней от корня до самого глубокого листа.        |
| **Рекурсия**                               | Функция вызывает саму себя для подсчёта высоты левого и правого поддерева. |
| **Базовый случай (`root == nil`)**         | Пустое дерево имеет высоту 0.                                              |
| **Параметры `leftHeight` и `rightHeight`** | Высоты левого и правого поддерева.                                         |
| **Выбор максимального**                    | Высота дерева = `1 + max(leftHeight, rightHeight)`.                        |
| **Лист дерева**                            | Узел без детей. Его высота = 1.                                            |

---

## Логика и пошаговый разбор кода

### 1. Объявление функции

```go
func BTreeLevelCount(root *TreeNode) int {
```

Функция принимает:

* `root` — корень дерева или текущего поддерева.

Возвращает:

* целое число — количество уровней (высота дерева).

---

### 2. Проверка: если узел пустой, высота = 0

```go
if root == nil {
	return 0
}
```

Это **базовый случай рекурсии**:

* пустого дерева нет → у него высота 0.

---

### 3. Рекурсивно считаем высоту левого и правого поддерева

```go
leftHeight := BTreeLevelCount(root.Left)
rightHeight := BTreeLevelCount(root.Right)
```

Алгоритм:

* спускаемся влево, пока не закончатся узлы → получаем высоту левого поддерева
* спускаемся вправо → получаем высоту правого поддерева

Каждый вызов возвращает высоту своего поддерева.

---

### 4. Выбираем большее поддерево

```go
if leftHeight > rightHeight {
	return leftHeight + 1
}
return rightHeight + 1
```

Пояснение:

* Высота дерева = **1 (текущий уровень) + высота самого высокого поддерева**
* Если левое поддерево ниже → добавляем +1 к нему
* Иначе → +1 к правому

То есть:

```
height(root) = 1 + max(height(left), height(right))
```

---

## Как это работает на примере

Дерево:

```
        4
      /   \
     1     7
          /
         5
```

Считаем:

### Узел `1`:

* левый nil → 0
* правый nil → 0
  → высота = 1

### Узел `5`:

* левый nil → 0
* правый nil → 0
  → высота = 1

### Узел `7`:

* левый = высота `5` = 1
* правый = nil = 0
  → высота = 1 + max(1,0) = 2

### Корень `4`:

* левый = высота `1` = 1
* правый = высота `7` = 2
  → высота = 1 + max(1,2) = 3

Ответ: **3**

---

## Глоссарий

| **Термин**                   | **Понятное объяснение**                                                |
| ---------------------------- | ---------------------------------------------------------------------- |
| **Высота дерева**            | Число уровней от корня до самого глубокого листа.                      |
| **Уровень (level)**          | «Слой» дерева: корень — уровень 1, его дети — уровень 2 и т.д.         |
| **Рекурсия**                 | Когда функция вызывает саму себя, пока не достигнет дна дерева.        |
| **Пустое дерево**            | Дерево без корня: его высота = 0.                                      |
| **Левое / правое поддерево** | Маленькое дерево, которое «вырастает» из левой или правой ссылки узла. |
| **Глубина**                  | Другой термин для описания уровней дерева.                             |
| **max(a, b)**                | Математическая операция выбора большего числа.                         |

---

## Заключение

Что нужно запомнить:

* Высота бинарного дерева → количество уровней.
* Алгоритм работает по формуле:

> **1 + max(высота левой ветки, высота правой)**

* Рекурсия идеально подходит для таких задач.
* Пустое дерево → уровень 0.
* Лист → уровень 1.
* Эта функция используется в проверках баланса дерева и анализе эффективности.
