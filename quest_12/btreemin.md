# Quest 12.10. btreemin

## Название файла Go

`btreemin.go`

---

## Условия задания

| **Instruction**                                                                                       | **Перевод**                                                                 |
| ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| *Write a function, BTreeMin, that returns the node with the minimum value in the tree given by root.* | *Напишите функцию, которая возвращает узел дерева с минимальным значением.* |

---

Нужно написать функцию, которая находит **минимальный элемент** в бинарном дереве поиска (BST).

📌 **Важный принцип BST:**

* У **самого левого узла** — минимальное значение.

Потому что:

* слева всегда лежат элементы меньше текущего узла,
* и мы идём влево, пока можем.

---

### Пример

Дерево:

```
        4
      /   \
     1     7
          /
         5
```

Минимальный узел → `1`

* **Вывод программы:**

```
1
```

---

## Решение

```go
package piscine

func BTreeMin(root *TreeNode) *TreeNode {
	if root == nil {
		return nil
	}

	current := root
	for current.Left != nil {
		current = current.Left
	}
	return current
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода**  | **Объяснение и назначение**                            |
| ------------------------------ | ------------------------------------------------------ |
| **BST (Binary Search Tree)**   | Структура: левое поддерево меньше узла, правое больше. |
| **Минимальный элемент**        | Самый левый узел в дереве.                             |
| **Поиск по левым узлам**       | Чтобы найти минимум в BST, нужно идти только влево.    |
| **Проверка `root == nil`**     | Если дерево пустое — минимального элемента нет.        |
| **Итеративный подход**         | Поиск минимума делается циклом, не рекурсией.          |
| **Возврат узла (`*TreeNode`)** | Функция возвращает *сам узел*, а не строку `Data`.     |

---

## Логика и пошаговый разбор кода

### 1. Объявление функции

```go
func BTreeMin(root *TreeNode) *TreeNode {
```

Функция принимает:

* `root` — корень дерева

Возвращает:

* указатель на минимальный узел или `nil`

---

### 2. Проверяем: дерево пустое?

```go
if root == nil {
	return nil
}
```

Если нет корня → возвращаем `nil`.

---

### 3. Начинаем поиск минимума

```go
current := root
```

`current` указывает на узел, с которого начинается поиск — это корень.

---

### 4. Идём влево, пока можем

```go
for current.Left != nil {
	current = current.Left
}
```

* В BST каждый левый узел → меньше текущего.
* Значит минимум находится **в самом левом узле**.

Алгоритм:

```
current = 4
→ есть левый? Да → идём в 1
current = 1
→ есть левый? Нет → это минимум
```

---

### 5. Возвращаем найденный узел

```go
return current
```

Это и есть сам узел, содержащий минимальное значение.

---

## Иллюстрация на конкретном дереве

Дерево:

```
        4
      /   \
     1     7
          /
         5
```

Алгоритм:

1. Start at 4 → есть левый → 1
2. At 1 → левого ребёнка нет → остановиться
3. Вернуть узел 1

Вывод:

```
1
```

---

## Глоссарий

| **Термин**            | **Понятное объяснение**                               |
| --------------------- | ----------------------------------------------------- |
| **Минимум в BST**     | Самый левый элемент дерева.                           |
| **Left child**        | Левый ребёнок узла. Всегда содержит меньшее значение. |
| **nil**               | Значение, означающее отсутствие узла.                 |
| **Итеративный обход** | Движение в цикле без рекурсии.                        |
| **Узел дерева**       | Структура `TreeNode` с данными и ссылками на детей.   |
| **Корень дерева**     | Самый верхний элемент BST.                            |

---

## Заключение

Что нужно запомнить:

* Минимум в бинарном дереве поиска всегда находится **в самом левом узле**.
* Чтобы его найти, нужно просто идти влево, пока возможно.
* Если дерево пустое → возвращаем `nil`.
* Важно возвращать именно **узел**, а не его значение — это нужно для дальнейших операций (удаление, поиск родителей, обход).
