# Quest 12.9. btreemax

## Название файла Go

`btreemax.go`

---

## Условия задания

| **Instruction**                                                                                       | **Перевод**                                                                  |
| ----------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| *Write a function, BTreeMax, that returns the node with the maximum value in the tree given by root.* | *Напишите функцию, которая возвращает узел дерева с максимальным значением.* |

---

Нужно написать функцию, которая находит **максимальный элемент** в бинарном дереве поиска (BST).

📌 **Важно:**

В BST максимальное значение всегда находится **в самом правом узле** дерева.

Почему?

* слева → всё меньше
* справа → всё больше

Поэтому мы просто спускаемся **вправо-вправо-вправо**, пока можем.

---

### Пример

* **Ввод:**

Дерево:

```
        4
      /   \
     1     7
          /
         5
```

* **Максимальный элемент:** `7`

* **Вывод:**

```
7
```

---

## Решение

```go
package piscine

func BTreeMax(root *TreeNode) *TreeNode {
	if root == nil {
		return nil
	}

	current := root

	for current.Right != nil {
		current = current.Right
	}
	return current
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода**       | **Объяснение и назначение**                                 |
| ----------------------------------- | ----------------------------------------------------------- |
| **BST (Binary Search Tree)**        | В правом поддереве значения всегда больше, чем в корне.     |
| **Максимальный элемент**            | Находится в узле, у которого *нет правого ребёнка*.         |
| **Поиск по правым детям**           | Простой способ найти максимум в BST — идти вправо до конца. |
| **Проверка `root == nil`**          | Пустое дерево → нет максимума → возвращаем `nil`.           |
| **Переменная `current`**            | Начинаем поиск с корня, затем спускаемся вправо.            |
| **Цикл `for current.Right != nil`** | Выполняется, пока справа существует узел.                   |
| **Возврат указателя на узел**       | Функция возвращает сам найденный узел, а не его значение.   |

---

## Логика и пошаговый разбор кода

### 1. Объявление функции

```go
func BTreeMax(root *TreeNode) *TreeNode {
```

Функция принимает:

* `root` — корень дерева
  и возвращает:

* узел (`*TreeNode`) с максимальным значением

---

### 2. Проверяем: дерево пустое?

```go
if root == nil {
	return nil
}
```

Если корня нет → нет максимального элемента.

---

### 3. Начинаем поиск с корня

```go
current := root
```

`current` — указатель на узел, от которого мы начинаем двигаться вправо.

---

### 4. Спускаемся вправо до конца

```go
for current.Right != nil {
	current = current.Right
}
```

Принцип:

* Пока существует правый потомок → идём к нему.
* В BST самое большое значение находится **в крайнем правом узле**.

Процесс на примере:

```
Start: 4
Right child exists → move to 7
Right child does NOT exist → stop
```

Останавливаемся на узле **7**.

---

### 5. Возвращаем найденный узел

```go
return current
```

`current` — это самый правый элемент → максимальный.

---

## Как это работает пошагово

Дерево:

```
        4
      /   \
     1     7
          /
         5
```

Шаги:

1. `current = root` → 4
2. Справа есть `7` → идём туда
3. Справа у `7` ничего нет → остановиться
4. Вернуть узел `7`

---

## Глоссарий

| **Термин**            | **Понятное объяснение**                               |
| --------------------- | ----------------------------------------------------- |
| **Максимальный узел** | Самый большой по значению элемент в дереве.           |
| **Правое поддерево**  | Часть дерева, где все значения больше значения корня. |
| **BST свойство**      | Лево < узел < право.                                  |
| **nil**               | Отсутствие узла.                                      |
| **Указатель на узел** | Функция возвращает именно узел, а не строку значения. |
| **Итеративный поиск** | Поиск с помощью цикла, без рекурсии.                  |

---

## Заключение

Что важно запомнить:

* В BST максимум всегда находится в **самом правом узле**.
* Достаточно идти вправо, пока это возможно.
* Код простой, быстрый и работает за **O(h)**, где *h — высота дерева*.
* В пустом дереве максимума нет → возвращаем `nil`.
