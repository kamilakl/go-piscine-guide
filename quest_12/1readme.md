# Quest 12 by kmukhtar

## Binary search tree - бинарные деревья поиска

Этот квест использует структуру данных - Binary search tree. Поэтому для начала я хочу разложить что это за структура данных и с чем ее едят.

---

## Что такое дерево?

Если абстрагироваться от программирования и просто представить дерево в жизни — это объект, у которого есть корень, ствол, ветви и листья.

**В программировании “деревом” называют структуру данных, в которой каждый элемент (узел) может иметь ноль или больше “потомков”, и при этом связи устроены так, что не образуют циклов.** То есть ты можешь двигаться от корня вниз, но никогда не вернёшься обратно по кругу.

### Основные термины дерева:

* **Root (корень)** — самый верхний элемент.
* **Node (узел)** — отдельный элемент данных.
* **Child (потомок)** — узел, идущий ниже.
* **Parent (родитель)** — узел, у которого есть children.
* **Leaf (лист)** — узел без детей.
* **Height (высота)** — длина максимального пути от корня до листа.

---

## Что такое бинарное дерево?

**Бинарное дерево — это дерево, где каждый узел может иметь максимум двух детей.**
Их называют:

* `Left` — левый ребёнок
* `Right` — правый ребёнок

То есть у каждого узла может быть:

* 0 детей
* 1 ребёнок
* 2 ребёнка

Иерархия выглядит примерно так:

```
       (10)
      /    \
    (5)    (15)
    / \     / \
  (2) (7) (12) (20)
```

---

## Что такое бинарное дерево поиска (Binary Search Tree, BST)?

Это частный случай бинарного дерева. У него есть **одно важное, золотое правило**:

> **Для любого узла верно: все значения в левом поддереве меньше значения этого узла, а в правом — больше.**

Гораздо проще понять на примере:

```
       8
      / \
     3   10
    / \    \
   1   6   14
      / \    \
     4   7    16
```

Здесь работают правила:

* в *левом поддереве* 8 — значения меньше 8
* в *правом поддереве* 8 — значения больше 8
* в *левом поддереве узла 6* — 4 (меньше 6)
* в *правом поддереве узла 6* — 7 (больше 6)

И так далее.

---

## Зачем вообще нужен Binary Search Tree?

BST — одна из самых полезных структур данных:

### 1️⃣ Быстрый поиск

Если дерево сбалансировано, time complexity:

* **Поиск:** O(log n)
* **Вставка:** O(log n)
* **Удаление:** O(log n)

### Почему в бинарном дереве поиска операции занимают O(log n)?

Начнём с того, что такое **O(log n)**. Большинство новичков боятся этой записи, хотя она означает очень простую идею.

### ✦ Что такое “отбрасывать половину элементов”?

Представь, что перед тобой телефонный справочник на 300 страниц.

Ты ищешь фамилию «Урунбаев».

Как ты будешь искать?

Ты же **не** начинаешь листать с первой страницы по одной.
Ты открываешь справочник **примерно посередине**.

* Если первая буква там — «К», значит «У» точно правее → отбрасываешь **всю левую половину**.
* Открываешь середину оставшейся части.
* Если попал на «Т», значит нужно правее → снова отбрасываешь половину.

То есть каждый раз количество оставшихся страниц уменьшается **в два раза**.

Вот это и есть логарифмическое время.

---

### Как это выглядит в бинарном дереве поиска?

Допустим, у тебя есть такое дерево:

```
           50
        /      \
      25        75
     /  \      /  \
   10   30    60   90
       /  \        / \
     28  35      80  100
```

Ты ищешь число **35**.

### Шаг 1. Смотришь на корень — 50

35 < 50 → значит нужное значение **точно в левом поддереве**.
Правую часть можно **полностью игнорировать**.

Отбрасываем половину.

### Шаг 2. Теперь ты в узле 25

35 > 25 → идём **направо**, а левую часть дерева выбрасываем.

Отбрасываем половину от половины.

### Шаг 3. Узел 30

35 > 30 → идём направо.

### Шаг 4. Узел 35 — найдено!

---

### Почему это быстрее, чем обычный перебор?

Если бы тебе пришлось искать число в обычном неотсортированном списке, ты бы смотрел элемент за элементом:

```
10 → не то  
50 → не то  
30 → не то  
60 → не то  
… и так далее  
```

Это называется **линейный поиск**, он работает за O(n).
Чем больше элементов, тем дольше.

---

### 📌 Но почему в BST всё так быстро?

Потому что дерево заранее построено так, что:

* всё меньшие значения — слева
* всё большие значения — справа

Каждый раз, когда ты переходишь влево или вправо, ты автоматически выбрасываешь **половину** всех возможных вариантов.

---

### Окей, а как понять разницу между O(n) и O(log n) буквально?

Представь, что у тебя есть **1024** элементов.

### При O(n)

В худшем случае — 1024 проверок.

### При O(log n)

Мы делим число на 2, пока оно не станет 1:

```
1024 → 512 → 256 → 128 → 64 → 32 → 16 → 8 → 4 → 2 → 1
```

Сколько шагов?

**10 шагов** вместо 1024.

Это и есть магия BST.

---

### Почему важно, что дерево “сбалансировано”?

Если дерево выглядит красиво и равномерно:

```
        50
      /    \
    25      75
   / \     / \
 ...
```

— шагов будет мало, всё быстро.

Но если ты вставляешь числа в кривом порядке, например:

```
1, 2, 3, 4, 5, 6
```

Дерево станет таким:

```
1
 \
  2
   \
    3
     \
      4
       \
        5
         \
          6
```

То есть превращается в **обычный длинный список**, и поиск снова становится O(n), потому что ты каждый раз идёшь только вправо.

Поэтому **BST работает быстро только если оно сбалансировано**.

---

Это называется **логарифмическая скорость — O(log n)**.
Но она работает только если дерево не превратилось в «палку».
Это похоже на бинарный поиск в массиве — ты каждый раз отбрасываешь половину элементов.

### 2️⃣ Быстрая сортировка

Если пройти дерево “в порядке” (`in-order traversal`), ты автоматически получаешь *отсортированную последовательность*.

### 3️⃣ Это основа многих структур:

* Красно-чёрные деревья
* AVL-деревья
* B-деревья
* Двоичная куча
* Карты и словари (в некоторых языках)

### 4️⃣ Лёгкая реализация поиска min/max

* Минимум — самый левый узел
* Максимум — самый правый узел

---

## Как выглядит узел BST в Go?

Структура максимально простая:

```go
type Node struct {
    Value int
    Left  *Node
    Right *Node
}
```

Каждый узел хранит:

* значение (`Value`)
* ссылку на левый узел (`Left`)
* ссылку на правый узел (`Right`)

---

## Как вставлять элементы в BST?

Вот правило, которое ты должен запомнить навсегда:

* Если новое значение **меньше** текущего узла — идём влево
* Если **больше** — направо
* Если дошли до пустого места — создаём новый узел

Пример кода:

```go
func Insert(node **Node, value int) {
    if *node == nil {
        *node = &Node{Value: value}
        return
    }

    if value < (*node).Value {
        Insert(&(*node).Left, value)
    } else if value > (*node).Value {
        Insert(&(*node).Right, value)
    }
}
```

Объяснение:

* принимаем указатель **на указатель** — чтобы можно было менять сам корень
* рекурсивно ищем подходящее место
* создаём новый узел, когда нашли пустое место

---

## Как искать элемент в BST?

Алгоритм похож на вставку:

```go
func Search(node *Node, target int) bool {
    if node == nil {
        return false
    }
    if node.Value == target {
        return true
    }
    if target < node.Value {
        return Search(node.Left, target)
    }
    return Search(node.Right, target)
}
```

То есть:

* если значение меньше — идём в левое дерево
* если больше — в правое

В худшем случае ты спустишься до листа.

---

## Как обходить дерево?

Есть три основных обхода:

### 1. In-order (лево → корень → право)

Результат — **отсортированный массив**.

```go
func InOrder(node *Node) {
    if node == nil {
        return
    }
    InOrder(node.Left)
    fmt.Print(node.Value, " ")
    InOrder(node.Right)
}
```

### 2. Pre-order (корень → лево → право)

Используют для сохранения структуры дерева.

### 3. Post-order (лево → право → корень)

Используют для удаления дерева.

---

## Как удалить элемент из BST?

Это самая сложная операция. Есть три случая:

### 1️⃣ Узел — лист (нет детей)

Просто удаляем.

### 2️⃣ Узел имеет одного ребёнка

Поднимаем ребёнка на место удаляемого узла.

### 3️⃣ Узел имеет двух детей

Ищем **минимальный элемент в правом поддереве**, заменяем им удаляемый узел.

Пример Go-кода:

```go
func Delete(node *Node, value int) *Node {
    if node == nil {
        return nil
    }

    if value < node.Value {
        node.Left = Delete(node.Left, value)
    } else if value > node.Value {
        node.Right = Delete(node.Right, value)
    } else {
        // нашли узел

        // 1. нет детей
        if node.Left == nil && node.Right == nil {
            return nil
        }

        // 2. один ребёнок
        if node.Left == nil {
            return node.Right
        }
        if node.Right == nil {
            return node.Left
        }

        // 3. два ребёнка
        min := findMin(node.Right)
        node.Value = min.Value
        node.Right = Delete(node.Right, min.Value)
    }

    return node
}

func findMin(n *Node) *Node {
    for n.Left != nil {
        n = n.Left
    }
    return n
}
```

---

## Реальный пример: использование BST в Go

Представь, что тебе нужно:

* хранить числа
* быстро искать, есть ли число в наборе
* выводить числа в отсортированном порядке

BST идеален.

Полный минимальный пример:

```go
package main

import "fmt"

type Node struct {
    Value int
    Left  *Node
    Right *Node
}

func Insert(node **Node, value int) {
    if *node == nil {
        *node = &Node{Value: value}
        return
    }
    if value < (*node).Value {
        Insert(&(*node).Left, value)
    } else if value > (*node).Value {
        Insert(&(*node).Right, value)
    }
}

func InOrder(n *Node) {
    if n == nil {
        return
    }
    InOrder(n.Left)
    fmt.Print(n.Value, " ")
    InOrder(n.Right)
}

func main() {
    var root *Node

    values := []int{8, 3, 10, 1, 6, 14, 4, 7}
    for _, v := range values {
        Insert(&root, v)
    }

    fmt.Println("Sorted values:")
    InOrder(root)
}
```

Вывод:

```
Sorted values:
1 3 4 6 7 8 10 14
```

---

## Где BST используется в реальной жизни?

* индексирование данных
* построение словарей
* хранение уникальных значений
* реализация autocomplete
* структура HTML-DOM (дерево)
* почти все компиляторы
* планировщики задач в операционных системах

---

## Почему важно понимать BST студенту Go?

Потому что ты:

* лучше поймёшь рекурсию
* поймёшь указатели
* поймёшь структурирование данных
* научишься писать алгоритмы с разветвлённой логикой
* научишься мыслить как инженер

BST — отличная тренировка программистского мозга.

---

## Итог

Бинарное дерево поиска — это фундаментальная структура данных, которую важно понимать всем, кто изучает алгоритмы или пишет серьёзный backend.

Она:

* проста на концептуальном уровне
* мощна по возможностям
* идеально подходит для демонстрации рекурсии и указателей
* активно используется в обучении структур данных

---

После того, как мы разобрались с бинарными деревьями поиска, хочу еще немного объяснить почему мы определяем тип данных TreeNode лишь однажды в самом первом файле. 

## 1. Что такое пакет в Go на самом деле

Когда ты пишешь:

```go
package piscine
```

в нескольких файлах (`btreeinsertdata.go`, `btreemin.go`, `btreemax.go`, `btreedeletenode.go` и т.д.),
Go видит **не набор отдельных файлов**, а **один логический модуль — пакет `piscine`**.

То есть:

* Файлы:

  * `btreeinsertdata.go`
  * `btreemin.go`
  * `btreemax.go`
  * `btreedeletenode.go`
* Все с `package piscine`

Для компилятора это выглядит как **одна большая программа**, просто разбитая по файлам.

---

## 2. Тип (структура) в пакете должен быть определён один раз

Когда ты пишешь:

```go
type TreeNode struct {
	Left, Right, Parent *TreeNode
	Data                string
}
```

ты говоришь:

> "В пакете `piscine` есть тип `TreeNode` вот такой формы".

И уже **все файлы этого пакета** знают про этот тип.

Если ты попытаешься во втором файле того же пакета ещё раз написать:

```go
type TreeNode struct {
	Left *TreeNode
	Data string
}
```

Go скажет:

> *"redeclared in this block"* — тип `TreeNode` уже объявлен

Потому что:

* **в одном и том же пакете** имя типа (`TreeNode`) может быть определено только один раз.

---

## 3. Почему мы не повторяем структуру в каждом файле

### ❌ Плохо:

```go
// btreeinsertdata.go
package piscine

type TreeNode struct { ... } // объявлено тут

// btreemin.go
package piscine

type TreeNode struct { ... } // и тут ещё раз — НЕЛЬЗЯ
```

Это приведёт к ошибке компиляции: **“type TreeNode redeclared in this block”**.

Плюс даже логически это плохо:

* Если бы Go позволил так делать, то в каждом файле у тебя был бы **СВОЙ собственный TreeNode**,
  и это были бы **разные типы**, не совместимые друг с другом.

То есть функция:

```go
func BTreeMin(root *TreeNode) *TreeNode
```

из одного файла **не смогла бы** работать с `TreeNode` из другого файла.

---

### ✅ Правильно:

Мы делаем **одну** структуру:

```go
// tree.go (или любой первый файл пакета)
package piscine

type TreeNode struct {
	Left, Right, Parent *TreeNode
	Data                string
}
```

А во всех остальных файлах мы просто её используем:

```go
// btreeinsertdata.go
package piscine

func BTreeInsertData(root *TreeNode, data string) *TreeNode {
    // ...
}
```

```go
// btreemin.go
package piscine

func BTreeMin(root *TreeNode) *TreeNode {
    // ...
}
```

```go
// btreedeletenode.go
package piscine

func BTreeDeleteNode(root, node *TreeNode) *TreeNode {
    // ...
}
```

Все эти функции работают **с одним и тем же типом** `TreeNode`.

---

## 4. Как Go “склеивает” все файлы пакета

Можно представить, что компилятор делает примерно так (упрощённо):

Он берёт:

```txt
btreeinsertdata.go
btreemin.go
btreemax.go
btreedeletenode.go
btreetransplant.go
...
```

все с `package piscine` — и **как будто** сливает их в один большой файл:

```go
package piscine

type TreeNode struct { ... }

// код из btreeinsertdata.go
// код из btreemin.go
// код из btreemax.go
// код из btreedeletenode.go
// код из btreetransplant.go
// и т.д.
```

Поэтому:

* достаточно **один раз** описать структуру `TreeNode`
* и она будет видна сразу всем функциям в этом пакете

С теорией разобрались, теперь идем писать задания из квеста!
