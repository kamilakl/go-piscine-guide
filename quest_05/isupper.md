# Quest 5.8 Is Upper

**Автор:** KeShMaNsKy  

---
## Название файла Go
`isupper.go`

---

## Условия задания 

| **Instruction** | **Перевод** |
|---------------------------|--------------|
| Write a function that returns true if the string passed as parameter contains only uppercase characters, otherwise, returns false. | Напиши функцию, которая возвращает `true`, если переданная строка содержит только заглавные буквы. В противном случае функция возвращает `false`. |

---

### Пример

- **Ввод:**
  ```go
  fmt.Println(piscine.IsUpper("HELLO"))
  fmt.Println(piscine.IsUpper("HELLO!"))
  ```
- **Вывод:**
  ```
  true
  false
  ```

Дополнительно:
- `IsUpper("AZ")` → `true`
- `IsUpper("az")` → `false`
- `IsUpper("-asdq-qcLEWQV-")` → `false`

---

## Решение

```go
package piscine

func IsUpper(s string) bool {
	for _, letter := range s {
		if letter < 'A' || letter > 'Z' {
			return false
		}
	}
	return true
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода** | **Объяснение и назначение** |
|-------------------------------|------------------------------|
| **Цикл `for _, letter := range s`** | Цикл `for range` перебирает все символы строки `s`. <br>• `letter` — это **руна (rune)**, то есть числовое представление символа в Unicode. <br>• `_` — индекс символа (мы его не используем). |
| **Условие (`if letter < 'A' || letter > 'Z'`)** | Проверяет, входит ли символ в диапазон заглавных латинских букв. Если не входит, сразу возвращает `false`. |
| **Символы `'A'` и `'Z'`** | Имеют числовые коды **65** и **90** в таблице ASCII. Проверка `letter < 'A' || letter > 'Z'` исключает все не-заглавные символы. |
| **`return false`** | Возвращает `false`, если найден хотя бы один символ, не соответствующий диапазону `A–Z`. |
| **`return true`** | Возвращается, если весь цикл прошёл без нарушений — значит, все символы заглавные. |
| **Тип `rune`** | Представляет один Unicode-символ (в Go это `int32`). Позволяет работать с символами, а не с байтами. |

---

## Логика и пошаговый разбор кода

### 1. Объявление функции
```go
func IsUpper(s string) bool {
```
Функция принимает строку `s` и возвращает `bool`.  
Результат будет `true`, если строка состоит только из заглавных букв, и `false` в противном случае.

---

### 2. Перебор символов строки
```go
for _, letter := range s {
```
Используется цикл `for range`, чтобы пройти по каждому символу строки.  
Каждый символ хранится в переменной `letter` в виде **руны (Unicode-код символа)**.

---

### 3. Проверка диапазона символов
```go
if letter < 'A' || letter > 'Z' {
	return false
}
```
Проверяется, находится ли символ в диапазоне заглавных латинских букв:
- `'A'` → 65  
- `'Z'` → 90  

Если хотя бы один символ не попадает в этот диапазон, функция сразу возвращает `false`.

---

### 4. Возврат результата
```go
return true
```
Если цикл завершился и не было найдено ни одного "неподходящего" символа, функция возвращает `true`.

---

## Важно знать

> В Go строки состоят из **байт**, а не символов.  
> Чтобы правильно проверять буквы, нужно работать с **рунами (`rune`)**, как это делает цикл `for range`.

Пример:
```go
for _, letter := range "HELLO" {
	fmt.Println(letter)
}
```
Выводит Unicode-коды:
```
72 69 76 76 79
```
Которые соответствуют символам `'H'`, `'E'`, `'L'`, `'L'`, `'O'`.

---

## Глоссарий

| **Термин** | **Пояснение** |
|-------------|----------------|
| **ASCII** | Таблица, в которой каждому символу присвоен уникальный числовой код (например, `'A'` = 65, `'Z'` = 90). |
| **Unicode** | Универсальный стандарт кодировки символов, включающий все языки мира. |
| **rune** | Тип данных (`int32`), представляющий один символ Unicode. |
| **bool** | Логический тип данных, принимающий значения `true` или `false`. |
| **if / return** | Конструкция для проверки условий и возврата результата из функции. |
| **for range** | Цикл для перебора элементов коллекции (строки, массива, карты и т.д.) в Go. |

---

## Заключение
Алгоритм:
1. Пройти по каждому символу строки.  
2. Проверить, находится ли символ между `'A'` и `'Z'`.  
3. Если хотя бы один символ не подходит — вернуть `false`.  
4. Иначе — вернуть `true`.

📘 **Примеры:**
- `IsUpper("HELLO")` → `true`  
- `IsUpper("HELLO!")` → `false`  
- `IsUpper("AZ")` → `true`  
- `IsUpper("az")` → `false`  
- `IsUpper("-asdq-qcLEWQV-")` → `false`
