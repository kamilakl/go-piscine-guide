# Quest 5.11 Is Numeric  
Автор: KeShMaNsKy  

## Название файла Go  
`isnumeric.go`

---
## Что нужно сделать — Instruction

| **Оригинал** | **Перевод** |
|---------------|--------------|
| *Write a function that returns `true` if the string passed as a parameter contains only numerical characters, otherwise, returns `false`.* | *Напиши функцию, которая возвращает `true`, если переданная в параметре строка содержит только числовые символы. В противном случае функция возвращает `false`.* |

---
## Решение

```go
package piscine

func IsNumeric(s string) bool {
	for _, letter := range s {
		if letter < '0' || letter > '9' {
			return false
		}
	}
	return true
}
```

---

## Ключевые понятия

| **Термин или инструмент** | **Объяснение** |
|----------------------------|----------------|
| **Тип `string`** | Последовательность символов (`rune`). В Go строки неизменяемы — каждая операция создаёт новую. <br>Подробнее: [Metanit — Строки в Go](https://metanit.com/go/tutorial/2.5.php) |
| **Тип `rune`** | Целое 32-битное значение (alias для `int32`), представляющее Unicode-символ. <br>Подробнее: [Metanit — Символы и коды Unicode](https://metanit.com/go/tutorial/2.5.php) |
| **Тип `bool`** | Логический тип: `true` (истина) и `false` (ложь). Используется в условиях. |
| **Оператор ИЛИ** | Логическое ИЛИ (две прямые палки): используем когда есть несколько условий и нужно проверить если хотя бы одно из условий истинно. |
| **Диапазон `'0'–'9'`** | Числовые коды символов: `'0'` = 48, `'9'` = 57. Все между ними это цифры. |
| **for range** | Цикл, который перебирает строку/срез/массив/карту/ по элементам. <br>Пример: `for _, ch := range "Go!"` вернёт **руны** `'G'`, `'o'`, `'!'`. <br>Подробнее: [For range в Golang](https://purpleschool.ru/knowledge-base/article/for-range-go) |

---
## Примеры для проверки

```go
package main

import (
	"fmt"
	"piscine"
)

func main() {
	fmt.Println(piscine.IsNumeric("010203"))
	fmt.Println(piscine.IsNumeric("01,02,03"))
	fmt.Println(piscine.IsNumeric("0123456789"))
	fmt.Println(piscine.IsNumeric("192.168.0.1"))
	fmt.Println(piscine.IsNumeric(""))
}
```

### 💻 Вывод:

```
true
false
true
false
true
```
---

## Логика и пошаговый разбор кода

### 1. Объявляем пакет и функцию

```go
package piscine 

func IsNumeric(s string) bool {
```

➡ Функция `IsNumeric` принимает строку `s` и возвращает логическое значение `true` или `false`.  
Ее цель - проверить состоит ли строка только из цифр.  
Возвращаемый тип `bool` выводит истина это или ложь.

---

### 3. Перебор всех символов строки

```go
for _, letter := range s {
```

➡ Цикл `for range` проходит по каждому символу строки `s`.  
- `_` — индекс(позиция символа) нам не нужен, поэтому ставится подчEркивание.  
- `letter` — сам символ в виде типа `rune` (числовой код Unicode).  

### 4. Проверка диапазона допустимых символов

```go
if letter < '0' || letter > '9' {
	return false
}
```
Здесь происходит проверка символа по диапазону `'0'–'9'`.

`'0'` в таблице Unicode имеет числовое значение **48**, а `'9'` **57**. Все, что между ними это цифры.  
Если символ меньше `'0'` или больше `'9'`, он не является цифрой.  

Здесь используется оператор `||` (ИЛИ):  если выполняется хотя бы одно из условий — символ *вне диапазона*.  

**Пример:**
| Символ | Условие | Результат |
|:-------|:--------|:-----------|
| `'5'` | `'0' ≤ '5' ≤ '9'` | ✅ цифра |
| `'A'` | `'A' > '9'` | ❌ не цифра |
| `'.'` | `'.' < '0'` | ❌ не цифра |

Как только встречается хотя бы один “запрещённый” символ (например `' '`, `'.'`, `'A'`, `','`),  
функция немедленно возвращает `false`, и проверка дальше не идёт.

---

### 5. Возврат результата

```go
return true
```

Если цикл закончился и не было найдено ни одного символа вне диапазона,  
значит строка состоит только из цифр, либо пустая - соответственно возвращаем `tru`

---

## 🧾 Заключение

Функция `IsNumeric` проверяет, состоит ли строка только из цифр `'0'–'9'`.  
Если встречается хотя бы один символ вне этого диапазона то `false`.  

Это пример простого валидатора, который можно использовать при проверке телефонных номеров, ID, PIN-кодов и любых других числовых данных.