# Quest 5.3 Nth Rune

## Название файла Go
`nrune.go`

---

## Условия задания 

| **Instruction** | **Перевод** |
|---------------------------|--------------|
| Write a function that returns the nth rune of a string. If not possible, it returns 0. | Напиши функцию, которая возвращает н-ную руну (символ) строки. Если это невозможно — функция возвращает 0. |

---

Нужно написать функцию, которая **возвращает символ с позицией n** в строке.  
Если такой позиции не существует (например, строка короткая или n меньше 1) — вернуть `0`.

### Пример

- **Ввод:**
  ```go
  z01.PrintRune(piscine.NRune("Hello!", 3))
  z01.PrintRune(piscine.NRune("Salut!", 2))
  z01.PrintRune(piscine.NRune("Bye!", -1))
  z01.PrintRune(piscine.NRune("Bye!", 5))
  z01.PrintRune(piscine.NRune("Ola!", 4))
  z01.PrintRune('\n')
  ```
- **Вывод:**  
  ```
  la!
  ```

---

## Решение

```go
package piscine

func NRune(s string, n int) rune {
	if n <= 0 {
		return 0
	}
	count := 0
	for _, r := range s {
		count++
		if count == n {
			return r
		}
	}
	return 0
}
```

---

## Ключевые понятия

| **Инструмент / элемент кода** | **Объяснение и назначение** |
|-------------------------------|------------------------------|
| **Пакет (`package piscine`)** | Каждая программа в Go делится на пакеты. `package` определяет, к какому модулю относится данный файл. Пакет `piscine` — это «папка с функциями», где все файлы могут использовать функции друг друга. |
| **Функция (`func NRune(s string, n int) rune`)** | Объявление функции. `func` сообщает компилятору, что далее идёт функция. Имя `NRune` начинается с заглавной буквы — значит функция экспортируется и доступна вне пакета. |
| **Параметры (`s string, n int`)** | Входные данные функции. `s` — строка, из которой нужно достать символ. `n` — порядковый номер символа, который нужно вернуть. |
| **Проверка условия (`if n <= 0`)** | Проверяет, не является ли номер символа нулевым или отрицательным. Если `n <= 0`, функция сразу возвращает `0`. |
| **Счётчик (`count := 0`)** | Переменная-счётчик, которая отслеживает позицию текущего символа в строке. Начинается с нуля и увеличивается в цикле. |
| **Цикл (`for _, r := range s`)** | Перебирает строку `s` посимвольно. `_` — индекс (не используется), `r` — текущая руна (символ). |
| **Инкремент (`count++`)** | Увеличивает счётчик на 1 при каждой итерации, чтобы знать, на каком символе мы сейчас. |
| **Проверка (`if count == n`)** | Когда номер текущего символа совпадает с `n`, функция возвращает этот символ и завершает работу. |
| **Возврат (`return 0`)** | Если цикл завершился, но `n` не найден, значит, строка слишком короткая. Функция возвращает `0`. |
| **Фигурные скобки `{ }`** | Обозначают блоки кода (функции, условия, циклы). Всё, что внутри скобок, принадлежит соответствующему блоку. |

---

## Логика и пошаговый разбор кода

### 1. Проверка корректности входных данных

```go
if n <= 0 {
	return 0
}
```

Если `n` меньше или равно нулю — это недопустимое значение.  
В строке не может быть нулевого или отрицательного индекса, поэтому возвращаем `0`.

---

### 2. Подготовка счётчика

```go
count := 0
```

Счётчик нужен, чтобы отслеживать номер символа при проходе по строке.  
На старте он равен 0, и будет увеличиваться на каждой итерации цикла.

---

### 3. Цикл по символам строки

```go
for _, r := range s {
	count++
	if count == n {
		return r
	}
}
```

- Цикл `for range` проходит по каждому символу строки.  
- На каждой итерации:
  - `r` получает текущий символ (руну).
  - `count` увеличивается на 1.
  - Если `count` совпадает с `n`, функция возвращает текущую руну `r`.

🧩 Пример для `"Hello!"`, `n = 3`:

| Итерация | Символ `r` | Счётчик `count` | Проверка `count == n` | Действие |
|-----------|-------------|-----------------|------------------------|-----------|
| 1 | `'H'` | 1 | Нет | продолжаем |
| 2 | `'e'` | 2 | Нет | продолжаем |
| 3 | `'l'` | 3 | ✅ Да | возвращаем `'l'` |

Функция завершает работу после нахождения символа.

---

### 4. Возврат по умолчанию

```go
return 0
```

Если цикл закончился, но нужный символ не найден, значит:
- `n` больше длины строки, или
- строка оказалась пустой.

В этом случае возвращается `0`.

---

## Возможные улучшения и пояснения

> 💡 Альтернативный способ:
> ```go
> runes := []rune(s)
> if n > 0 && n <= len(runes) {
>     return runes[n-1]
> }
> return 0
> ```
> Этот вариант создаёт срез рун и напрямую обращается к нужному индексу.  
> Однако изначальный вариант с циклом не требует лишнего преобразования строки и является более наглядным для обучения.

---

## Глоссарий

| **Термин** | **Понятное объяснение** |
|-------------|--------------------------|
| **rune** | Тип данных для представления одного символа Unicode. |
| **string** | Последовательность символов (строка). |
| **range** | Ключевое слово для перебора элементов в коллекции (строки, массива и т. д.). |
| **if** | Условный оператор для проверки условий. |
| **count** | Счётчик, показывающий, какой символ сейчас рассматривается. |
| **return** | Команда для возврата значения из функции. |
| **0 (нулевая руна)** | Пустой символ — используется, когда ничего не найдено. |
| **package** | Группа файлов и функций в Go. |
| **int** | Тип данных для хранения целых чисел. |
| **len()** | Функция, возвращающая длину (количество элементов). |

---

## Заключение

Функция **NRune** проходит по строке посимвольно и возвращает **символ с позицией n**.  
Если такого символа нет (например, индекс выходит за пределы строки или n отрицателен), она возвращает `0`.

📘 Примеры работы:
- `("Hello!", 3)` → `'l'`  
- `("Salut!", 2)` → `'a'`  
- `("Bye!", -1)` → `0`  
- `("Ola!", 4)` → `'!'`

Таким образом, **NRune** безопасно работает с Unicode-строками и позволяет получить конкретную руну по её порядковому номеру.
