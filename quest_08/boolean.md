# 📝 Quest 8.1 Boolean

## Название файла Go
`main.go`

---

## Условия задания 

| **Instruction** | **Перевод** |
|:------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------|
| **Create a new directory called `boolean`.** | Создайте новую директорию с именем `boolean`. |
| **The code below must be copied into a file called `main.go` inside of the `boolean` directory.** | Приведенный ниже код должен быть скопирован в файл с именем `main.go` внутри директории `boolean`. |
| **The necessary changes must be applied for the program to work.** | Необходимо внести необходимые изменения, чтобы программа заработала. |

---

Нужно написать программу на Go, которая **определяет, является ли количество аргументов командной строки (исключая название самой программы) четным или нечетным**, и выводит соответствующее сообщение. Для этого необходимо **отредактировать** предоставленный "сломанный" код, чтобы он соответствовал синтаксису Go и логике работы с аргументами.

### Пример

- **Ввод:** `go run . "not" "odd"`
- **Вывод:** `I have an even number of arguments`
- **Ввод:** `go run . "not even"`
- **Вывод:** `I have an odd number of arguments`

---

## Решение



```go
package main

import (
	"os"
	"[github.com/01-edu/z01](https://github.com/01-edu/z01)"
)

// Эта функция printStr была предоставлена в задании и использует z01.PrintRune для вывода строки
func printStr(s string) {
	for _, r := range s {
		z01.PrintRune(r)
	}
	z01.PrintRune('\n')
}

// isEven - функция, которую нужно было исправить.
// Она принимает целое число nbr (количество аргументов) и возвращает bool (истина/ложь).
func isEven(nbr int) bool {
	// Проверка на четность. Оператор % (остаток от деления) возвращает 0,
	// если число четное.
	if nbr%2 == 0 {
		return true // Четное число
	}
	return false // Нечетное число
}

func main() {
	// os.Args - это срез, содержащий все аргументы, включая имя программы (os.Args[0]).
	// arguments := os.Args[1:] создает срез, который содержит только аргументы,
	// переданные пользователем.
	arguments := os.Args[1:]

	// Проверяем, является ли длина среза arguments четной.
	if isEven(len(arguments)) {
		printStr("I have an even number of arguments")
	} else {
		printStr("I have an odd number of arguments")
	}
}
```

## Ключевые понятия

| **Инструмент / элемент кода** | **Объяснение и назначение** |
|:------------------------------|:----------------------------------------------------------------------------------------------------------------------------------|
| **Пакет (`package ...`)** | `package main` - объявление основного пакета программы. |
| **Функция (`func ...`)** | `func isEven(nbr int) bool` - объявление функции, принимающей целое число и возвращающей булево значение. |
| **`os` (пакет)** | Предоставляет доступ к функциям операционной системы, включая аргументы командной строки (`os.Args`). |
| **`os.Args`** | **Срез строк**, содержащий аргументы командной строки. Первый элемент (`os.Args[0]`) — это имя запущенной программы. |
| **`os.Args[1:]`** | Синтаксис **слайсинга** (slicing) в Go, исключающий имя программы. |
| **`len(...)`** | Встроенная функция Go, возвращающая **длину** среза. |
| **`bool` (тип данных)** | **Булевый тип** данных в Go (`true` / `false`). |
| **Оператор `%` (Modulus)** | **Оператор остатка от деления**, используется для проверки четности. |

## Логика и пошаговый разбор кода

### 1. Объявление функции

### 1. Объявление и Импорт пакетов

```go
package main

import (
	"os"
	"[github.com/01-edu/z01](https://github.com/01-edu/z01)"
)
```
 Пакет main требуется, так как это исполняемая программа. Импорт os необходим для доступа к аргументам командной строки (os.Args).

---

### 2. Исправление функции `isEven`

```go
func isEven(nbr int) bool {
	if nbr%2 == 0 {
		return true 
	}
	return false 
}
```
 **Ключевое исправление**: Замена несуществующего типа boolean на bool и значений yes/no на true/false. Функция теперь корректно проверяет четность.

---

### 3. Извлечение фактических аргументов

```go
	arguments := os.Args[1:]
```
 Внутри main() мы используем слайсинг ([1:]), чтобы создать срез arguments, который содержит только аргументы, переданные пользователем, исключая имя программы (os.Args[0]).

 ---

 ### 4. Вычисление и вывод результата

```go
	if isEven(len(arguments)) {
		printStr("I have an even number of arguments")
	} else {
		printStr("I have an odd number of arguments")
	}
```

 Мы вызываем **len(arguments)**, чтобы получить количество аргументов пользователя. Результат передается в функцию **isEven()**. В зависимости от возвращенного булева значения (true или false) выводится сообщение.

---

### 📚 Глоссарий 


| **Термин** | **Понятное объяснение** |
|:-------------|:--------------------------|
| **Командная строка (CLI)** | Интерфейс взаимодействия с программой путем ввода текстовых команд. |
| **Аргументы (Arguments)** | Значения, которые передаются программе при ее запуске после ее имени. |
| **Срез (Slice)** | Динамически изменяемый массив в Go. `os.Args` является срезом строк. |
| **Булева логика** | Раздел математической логики, оперирующий только двумя значениями: "истина" (`true`) и "ложь" (`false`). |
| **`Modulus` (Модуль)** | Операция, вычисляющая остаток от деления одного числа на другое (обозначается `%`). |

--- 

## Заключение

- **Главное изменение:** Замена неправильного синтаксиса на стандартные типы Go: **`bool`**, **`true`**, и **`false`**.
- **Ключевой метод:** Использование **`os.Args[1:]`** для правильного подсчета аргументов командной строки, игнорируя имя программы.
- **Проверка четности:** Использование простого и эффективного **оператора Modulus (`%`)**.